//Utility class for Query. Handles calculations assosciated with ranking query results.

package models;
/*

class QueryUtility{


	public int getPageRankScore(String documentId){
		//Reads through citegeist.txt and finds the score assosciated with a particular documentId.
		//Returns score if documentId is found, -1 if not.
		BufferedReader br = new BufferedReader(new FileReader(""));

		String line;
		String[] temp;
		while((line = br.readLine()) != null){
			//split line based on "=".
			temp = line.split("=");
			//temp[0] has document id, temp[1] has the score.
			if(temp[0] == documentId){
				//Found. Return score.
				return temp[1];
			}
		}
	

		return -1;
	}





}
*/
